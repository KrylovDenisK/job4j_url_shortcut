server:
  port: 8080
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL95Dialect
    hibernate:
      ddl-auto: validate
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/urlshortcut
    username: postgres
    password: 132197
  liquibase:
     change-log: classpath:db/changelog/db.changeLog-master.xml